# ===================================================================
# NeuroVoice - Parkinson's Detection System Requirements
# Compatible con Python 3.8+ (Recomendado: Python 3.10+)
# ===================================================================

# === Core Scientific Computing ===
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.10.0,<2.0.0

# === Audio Processing ===
librosa>=0.10.0,<1.0.0
soundfile>=0.12.0,<1.0.0
audioread>=3.0.0,<4.0.0
resampy>=0.4.0,<1.0.0
sounddevice>=0.4.0,<1.0.0  # Para grabación en directo

# === Machine Learning & Deep Learning ===
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
scikit-learn>=1.3.0,<2.0.0

# === Data Visualization ===
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
mplcursors>=0.5.0,<1.0.0

# === Progress & Utilities ===
tqdm>=4.65.0,<5.0.0

# === Performance Optimization ===
numba>=0.57.0,<1.0.0

# === GUI & Reporting ===
PyQt6>=6.4.0,<7.0.0
reportlab>=3.6.0,<4.0.0

# ===================================================================
# DEPENDENCIAS INCLUIDAS EN PYTHON (No requieren instalación):
# - pathlib (Built-in desde Python 3.4+)
# - json (Built-in)
# - warnings (Built-in)
# - time (Built-in)
# - os (Built-in)
# - sys (Built-in)
# - logging (Built-in)
# - datetime (Built-in)
# - argparse (Built-in)
# - typing (Built-in desde Python 3.5+)
# - shutil (Built-in)
# - random (Built-in)
# ===================================================================

# === Optional: Development & Testing ===
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# === Optional: Jupyter Notebook Support ===
jupyter>=1.0.0,<2.0.0
ipykernel>=6.0.0,<7.0.0

# ===================================================================
# INSTALACIÓN:
# pip install -r requirements.txt
#
# ENTORNO VIRTUAL (Recomendado):
# python -m venv venv
# source venv/bin/activate  # En Linux/Mac
# venv\Scripts\activate     # En Windows
# pip install -r requirements.txt
#
# VERIFICAR INSTALACIÓN:
# python -c "import torch, librosa, sounddevice, PyQt6, reportlab, mplcursors; print('✅ Todas las dependencias instaladas')"
# ===================================================================
